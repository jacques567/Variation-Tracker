#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running Playwright tests before commit..."
npm run test:e2e

if [ $? -ne 0 ]; then
  echo "❌ Tests failed. Commit aborted."
  exit 1
fi

echo "✅ All tests passed. Proceeding with commit."
